name: Cold Chain Data Generator

on:
  schedule:
    - cron: "0 0 * * *"   # runs once every day at 00:00 UTC
  workflow_dispatch:     # allows manual run too

jobs:
  generate-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run Full Synthetic Generator
        run: |
          python full_synthetic_generator.py

      - name: Run Sensor Validation Agent
        run: |
          python SensorValidationAgent.py

      - name: Commit generated files
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add *.xlsx
          git commit -m "Daily generated and validated data" || echo "No changes"
          git push
